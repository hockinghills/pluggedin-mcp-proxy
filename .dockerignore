# Git files
.git
.gitignore

# Node modules
node_modules

# Build artifacts (if building outside Docker first)
dist 
# Keep dist if using multi-stage build where source is copied first

# Source files are needed for the build step inside Docker
# src

# Test files
tests/
*.test.ts
*.spec.ts
vitest.config.ts
jest.config.js

# Configuration files
# tsconfig.json is needed for the build step inside Docker
# tsconfig.json
.env*
pnpm-lock.yaml # Keep if running pnpm install in Dockerfile
package-lock.json # If npm is used
yarn.lock # If yarn is used
.clinerules

# Docker files themselves
Dockerfile
docker-compose.yml

# Other development files
README.md
LICENSE
.npmignore
